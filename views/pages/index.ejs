<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs')%>
</head>
<body>
    <%- include('../partials/header.ejs') %>
    <main class="home">
        <h1></h1>
        <section class="trending">
            <h2>What to watch next</h2>
                <ul>
                    <% movies.forEach(movie=> { %>
                        <li>
                            <section class="poster">
                                <% if (movie.poster_path) { %>
                                    <a href="/movie/<%= movie.id %>"><img src="https://image.tmdb.org/t/p/w500<%= movie.poster_path %>" alt="<%= movie.title %>" class="moviePoster"></a>
                                    <% } else { %>
                                        <img src="/images/logo.png" class="" alt="">
                                        <% } %>
                            </section> 
                            <% if (movie.title) { %>
                                <a href="/movie/<%= movie.id %>"><%= movie.title %>
                                    <% } else { %>
                                        <a href="/404">Movie not found</a>
                                    <% } %>
                        </li>
                    <% }); %>
                </ul>
        </section>
        <section class="popular">
            <h2>Trending series</h2>
                <ul>
                    <% series.forEach(serie=> { %>
                        <li>
                            <section class="poster">
                                <% if (serie.poster_path) { %>
                                    <img src="https://image.tmdb.org/t/p/w500<%= serie.poster_path %>" alt="<%= serie.name %>">
                                    <% } else { %>
                                        <img src="/images/logo.png" class="" alt="">
                                        <% } %>
                            </section> 
                            <% if (serie.name) { %>
                                <a href="/movie/<%= serie.id %>"><%= serie.name %></a>
                                    <% } else { %>
                                        <a href="/404">Serie not found</a>
                                    <% } %>
                        </li>
                    <% }); %>
                </ul>
        </section>
    </main>
    <%- include('../partials/footer.ejs') %>
    <script src="/scripts/script.js"></script>
</body>
</html>